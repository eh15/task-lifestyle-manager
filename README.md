To run, type `npm start`

Personal project, env files are not provided

To-do list:
- find a hosting service for bot 100% uptime
  - can implement more complex random features (random task-list reminder, random message)
  - can implement curfew at specific time
